# 🚀 راهنمای سریع شروع کار

## مرحله ۱: نصب Node.js

اگر Node.js روی سیستم شما نصب نیست:
1. به سایت https://nodejs.org بروید
2. نسخه LTS را دانلود و نصب کنید
3. برای چک کردن نصب، در ترمینال تایپ کنید:
```bash
node --version
```

## مرحله ۲: نصب پروژه

1. پوشه `portfolio` را در جایی که می‌خواهید باز کنید
2. ترمینال را در همین پوشه باز کنید
3. دستور زیر را اجرا کنید:
```bash
npm install
```

این کار تمام پکیج‌های مورد نیاز را نصب می‌کند (حدود 2-3 دقیقه طول می‌کشد)

## مرحله ۳: اجرای پروژه

```bash
npm run dev
```

سایت شما در آدرس http://localhost:3000 اجرا می‌شود

## 🎨 شخصی‌سازی سریع

### 1. تغییر نام و اطلاعات شخصی

فایل `src/lib/translations.ts` را باز کنید و این قسمت‌ها را ویرایش کنید:

```typescript
hero: {
  name: 'نام شما',  // ← نام خودت رو بنویس
  // ...
}
```

### 2. اضافه کردن نمونه کار

فایل `src/components/Portfolio.tsx` را باز کنید:

```typescript
const projects = [
  {
    id: 1,
    title: 'نام پروژه',
    category: 'branding', // یا logo, print, digital
    image: '/images/project1.jpg', // آدرس عکس
    description: 'توضیحات پروژه',
  },
  // پروژه‌های بیشتر...
];
```

### 3. تغییر اطلاعات تماس

فایل `src/components/Contact.tsx`:

```typescript
const contactInfo = [
  {
    icon: FiMail,
    title: 'Email',
    value: 'your.email@example.com', // ← ایمیل خودت
  },
  {
    icon: FiPhone,
    title: 'تلفن',
    value: '+98 912 345 6789', // ← شماره خودت
  },
  // ...
];
```

## 📸 آپلود تصاویر

### روش ۱: آپلود در پروژه
1. پوشه `public/images/` بسازید
2. عکس‌هایت رو توش بریز
3. تو کد از `/images/photo.jpg` استفاده کن

### روش ۲: استفاده از سرویس آنلاین
1. برو https://imgbb.com
2. عکست رو آپلود کن
3. لینک رو کپی کن و تو کد بذار

## 🎨 تغییر رنگ‌ها

رنگ‌ها تو کد با این فرمت هستند:
```typescript
className="bg-gradient-to-r from-purple-600 to-pink-600"
```

می‌تونی عوض کنی به:
- `from-blue-600 to-cyan-600` (آبی)
- `from-green-600 to-teal-600` (سبز)
- `from-red-600 to-orange-600` (قرمز-نارنجی)

## 🌍 انتشار سایت

### Vercel (رایگان و آسان)

1. برو https://vercel.com
2. ثبت نام کن (با GitHub)
3. روی "Import Project" کلیک کن
4. پوشه پروژه رو انتخاب کن
5. Deploy!

سایت شما آنلاین میشه و یه آدرس بهتون میده مثل:
`your-portfolio.vercel.app`

## ❓ مشکلات رایج

### ارور "Cannot find module"
```bash
npm install
```

### سایت لود نمیشه
- مطمئن شو Node.js نصب باشه
- پورت 3000 آزاد باشه
- ترمینال رو ببند و دوباره باز کن

### عکس‌ها نمایش نمیشن
- مسیر عکس رو چک کن
- اگه از لینک خارجی استفادی می‌کنی، در `next.config.js` دامنه رو اضافه کن:

```javascript
images: {
  domains: ['example.com'],
}
```

## 💡 نکات مهم

1. ✅ همیشه قبل از تغییرات، یه کپی بگیر
2. ✅ بعد از هر تغییر، صفحه رو Refresh کن
3. ✅ از تصاویر با کیفیت و حجم مناسب استفاده کن
4. ✅ قبل از انتشار، سایت رو تو موبایل تست کن

## 📞 نیاز به کمک؟

اگه جایی گیر کردی:
1. README.md رو بخون
2. تو گوگل سرچ کن
3. از ChatGPT کمک بگیر!

---

موفق باشی! 🎉
